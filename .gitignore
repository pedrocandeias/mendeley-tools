# Python
__pycache__/
*.py[cod]

# Credentials and local settings — never commit these
.env
.env.local

# Local run artefacts. This repository holds tools only: the PDF collection,
# the cached library dumps and the generated reports belong to whichever
# project the tools are pointed at.
*_cache.json
mendeley_docs*.json
*.pdf
*.zip
*.csv
report*.md
audit*.md

# LibreOffice lock files
.~lock.*#
